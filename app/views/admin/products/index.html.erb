<% content_for :title, "Products" %>

<section class="admin__content flex flex-col pb-2">
  <%= render 'admin/page_header', 
    new: { path: new_admin_product_path, title: "Add New Product" }, 
    ransack: { 
      q: @q, 
      url: admin_products_path, 
      search_box: [ { searchables: :name_or_description_cont } ] 
    } 
  %>

  <div class="section__table--container full-height px-2">
    <% if @admin_products.any? %>
      <%= render partial: 'shared/table', 
        locals: { 
          headers: @table_headers, 
          list: @admin_products, 
          pagy: @admin_products_pagy, 
          actions: @table_actions, 
          class_names: "full-height px-2" 
        } 
      %>
    <% else %>
      <%= turbo_frame_tag :results, class: "p-2" do %>
        <% if params[:q].present? %>
          <p class="text-center">No products found matching your search criteria.</p>
        <% else %>
          <p class="text-center">No products available.</p>
        <% end %>
      <% end %>
    <% end %>
  </div>
</section>

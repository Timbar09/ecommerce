<% content_for :title, "Categories" %>

<section class="admin__content flex flex-col pb-2">
  <%= render 'admin/page_header', path: new_admin_category_path, title: "Add New Category" %>

  <div class="section__table--container full-height px-2">
    <%= render partial: 'shared/table', locals: { headers: @table_headers, list: @admin_categories, actions: @table_actions, class_names: "px-2" } %>
  </div>
</section>
